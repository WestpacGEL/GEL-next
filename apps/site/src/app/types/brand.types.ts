// TO DO: Check with designers if btfg brand is still supported. BrandKey exported from ui package contains it but it is not currently on the website and is messing up types
export type BrandKey = 'bom' | 'bsa' | 'stg' | 'wbc';
