A successful editing experience:

- Allows customers to complete changes confidently
- Uses the lightest model that safely supports the task
- Maintains consistency across channels
- Uses platform-appropriate implementations while remaining aligned to the enterprise model
- Considers accessibility, validation and recovery from the start
- Can be reused and scaled beyond a single feature or team
