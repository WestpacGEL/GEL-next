Choose the lightest-weight editing model that can safely support the task.

The appropriate model should be determined by:

- Customer risk
- Task complexity
- Validation requirements
- Dependencies between fields
- Guidance and support needs

Teams should identify the editing model first, then select the most appropriate platform-specific component or interaction pattern.
