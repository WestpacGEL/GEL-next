Example with a button on the right

```tsx
<>
  <Header brand="wbc">
    <Button
      look="faint"
      onClick={function Ya(){}}
      size={{
        initial: 'small',
        sm: 'medium'
      }}
      soft
    >
      Sign Out
    </Button>
  </Header>
</>
```
