The GEL is separated out into two packages:

- **ui**: Contains all the design system components
- **style-config**: Provides the tailwind css themes and design tokens

Install the packages into your project

```html
<StaticCode language="js" code={`npm i @westpac/ui @westpac/style-config`} />
```
