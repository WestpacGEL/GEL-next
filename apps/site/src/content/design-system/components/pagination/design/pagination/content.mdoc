Use this pagination component for navigation within both finite (a set number) and infinite (unknown number) of views.

```tsx
<Pagination
  current={1}
  onChange={function Ma(){}}
  onPageItemProps={function Ma(){}}
  totalPages={20}
/>
```
