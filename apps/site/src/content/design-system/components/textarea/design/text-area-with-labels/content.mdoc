Use the [Input group](/components/input-group) component to accessibly define the labels used with [Inputs](/components/inputs), and Text areas. All inputs require labels for usability and accessibility.

```jsx
  <InputGroup width={20} label="Label" hint="Hint text" >
          <Textarea />
        </InputGroup>
```
