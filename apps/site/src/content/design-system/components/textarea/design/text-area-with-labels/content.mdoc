Use the [Input group](/components/input-group) component to accessibly define the labels used with Text areas and [Inputs](/components/inputs). All inputs require labels for usability and accessibility.

```jsx
  <InputGroup label="Label" hint="Hint text" >
          <Textarea />
        </InputGroup>
```
