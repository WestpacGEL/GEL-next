The Body component is a wrapper component to define an area of body text content. The component provides text styling, importantly the styling of text links. A number of components leverage this component to provide consistent text styling.
