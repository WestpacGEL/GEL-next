import { tv } from 'tailwind-variants';

export const styles = tv({
  slots: {
    contentContainer: 'bg-background-white-faint mb-5 mt-4 max-w-5xl p-6 pb-0',
    tableContainer: 'border-border-muted-soft relative -mx-6 -mt-6 rounded-xl border px-6 pb-6 pt-9',
  },
});
