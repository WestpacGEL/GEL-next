import { tv } from 'tailwind-variants';

export const styles = tv({
  slots: {
    contentContainer: 'bg-background-white-faint mt-4 mb-5 max-w-5xl rounded-3xl p-6 pb-0',
    tableContainer: 'border-border-muted-soft relative -mx-6 -mt-6 rounded-3xl border px-6 pt-9 pb-6',
  },
});
