import { tv } from 'tailwind-variants';

export const styles = tv({
  slots: {
    contentContainer: 'mb-5 mt-4 max-w-5xl bg-background-white-faint p-6 pb-0',
    tableContainer: 'relative -mx-6 -mt-6 rounded-xl border border-border-muted-soft px-6 pb-6 pt-9',
  },
});
