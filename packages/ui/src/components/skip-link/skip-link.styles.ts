import { tv } from 'tailwind-variants';

export const styles = tv({
  base: 'sr-only focus:not-sr-only focus:fixed focus:inset-x-0 focus:top-0 focus:bg-background-white-pale focus:p-3 focus:text-center focus:text-xl focus:text-text-link focus:underline focus:focus-outline focus:-outline-offset-2',
});
