import { tv } from 'tailwind-variants';

export const styles = tv({
  base: 'sr-only focus:not-sr-only focus:fixed focus:inset-x-0 focus:top-0 focus:bg-white focus:p-3 focus:text-center focus:text-xl focus:text-link focus:underline focus:-outline-offset-2',
});
