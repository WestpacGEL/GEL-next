import { tv } from 'tailwind-variants';

export const styles = tv({
  slots: {
    base: '-mx-1 mt-4 mb-3 bg-surface-muted-faint p-[0.4375rem] typography-body-10 font-medium text-text-hero first-of-type:-mt-1',
  },
});
