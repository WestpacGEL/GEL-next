import { tv } from 'tailwind-variants';

export const styles = tv({
  slots: {
    base: 'typography-body-10 bg-surface-muted-faint text-text-hero -mx-1 mt-4 mb-3 p-[0.4375rem] font-medium first-of-type:-mt-1',
  },
});
