import { tv } from 'tailwind-variants';

export const styles = tv({
  slots: {
    button: 'flex size-4 shrink-0 cursor-pointer items-center justify-center rounded-sm focus-visible:focus-outline',
  },
});
