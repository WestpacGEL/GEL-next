import { tv } from 'tailwind-variants';

export const styles = tv({
  base: 'absolute top-full left-1 z-10 mt-2 rounded-sm border border-border-muted-soft bg-surface-muted-faint p-0.5 whitespace-nowrap text-text-body',
});
