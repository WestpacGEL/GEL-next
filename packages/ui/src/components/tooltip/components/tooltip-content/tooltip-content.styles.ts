import { tv } from 'tailwind-variants';

export const styles = tv({
  base: 'border-border-muted-soft bg-surface-muted-faint text-text-body absolute top-full left-1 z-10 mt-2 rounded-sm border p-0.5 whitespace-nowrap',
});
