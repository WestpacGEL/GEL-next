import colors from '../../../flattened-tokens.json' with { type: 'json' };

export const theme = {
  code: 'STG-LIGHT',
  colors: colors.StGeorge.light.color,
  brandFont: 'Dragon Bold',
  name: 'St.George',
} as const;
