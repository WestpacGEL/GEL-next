import colors from '../../../flattened-tokens.json' with { type: 'json' };

export const theme = {
  code: 'STG-DARK',
  colors: colors.StGeorge.dark.color,
  brandFont: 'Dragon Bold',
  name: 'St.George',
} as const;
